# Your agent's dependencies go here.
#
# These get combined with the cloud-specific runtime deps (under aws/ or gcp/)
# at build time. You don't need to include `bedrock-agentcore` or
# `google-cloud-aiplatform` — those are added automatically.

# ── Default example (LangGraph ReAct) ───────────────────────────────────────
langchain-core
langchain-openai
langgraph

# ── Optional: Arize Phoenix observability ───────────────────────────────────
# Uncomment to enable the instrumentation block in src/invocation.py.
# Pick the instrumentor that matches your framework.
# arize-phoenix-otel
# openinference-instrumentation-langchain
# openinference-instrumentation-crewai
# openinference-instrumentation-openai

# ── If you swap src/invocation.py for a different example, add its deps here ─────
# CrewAI:    crewai
# Raw OpenAI:  openai
# LangChain AgentExecutor:  langchain
